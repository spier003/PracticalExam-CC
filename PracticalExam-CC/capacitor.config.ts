import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PracticalExam-CC',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
